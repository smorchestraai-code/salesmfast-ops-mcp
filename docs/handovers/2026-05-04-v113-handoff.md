# v1.1.3 handoff — paste into SMO-236 and SMO-248 comments

> Linear MCP was disconnected during the session that shipped v1.1.3, so this comment is
> staged here. Paste verbatim into both tickets when Linear is reachable.

---

## v1.1.3 shipped — closes the 6 defects from your live-session QA on v1.1.2 ✅

You ran 25 ops on v1.1.2; pass rate was 68% with 6 high-severity defects. Triage showed 2 of them (D-01 create-tag, D-02 list-tags) were transient session-state cascades — the underlying ops work fine empirically. The other 4 were real upstream bugs and are now bypassed at the facade layer via direct axios calls.

**Tag:** `v1.1.3-search-bypass`
**PR:** https://github.com/smorchestraai-code/salesmfast-ops-mcp/pull/13 (merged)
**Probes:** 32 read-path + 4 write-path = 36/36 GREEN

### What's new in v1.1.3 vs v1.1.2

| Defect | v1.1.2 behaviour | v1.1.3 fix |
|---|---|---|
| D-04 contacts.search filters | `filters: {tags: [...]}` silently dropped (only email/phone forwarded). Tag filtering returned ALL 32k contacts unfiltered | Filters honored — array clauses passed through; convenience object converted; top-level `tags`/`email`/`phone`/`dateAdded` hoisted |
| D-05 contacts.search pageLimit | Hard-capped at 25 regardless of value | `pageLimit: 100` returns 100; default still 25 |
| D-06 contacts.search cursor | `startAfterId`/`startAfter` silently dropped — no pagination beyond first page | Cursor pagination works; pass last contact's `id` + `dateUpdated` millis from prior page |
| D-03 survey.list-submissions | `404 Cannot GET /locations/{id}/surveys/submissions` — wrong upstream URL | Calls correct GHL v2 endpoint `/surveys/submissions?locationId=...`; returns clean `{ submissions, meta }` envelope |
| G-04 survey description | Claimed "AND forms" but SurveyTools doesn't wrap forms | Description corrected — surveys only |

### Steps for you to re-pull on every Mac

```bash
cd ~/Desktop/salesmfast-ops-mcp           # or wherever you cloned
git fetch --tags
git checkout v1.1.3-search-bypass
npm install
npm run build
```

Then **fully quit Claude Desktop** (`Cmd+Q`, not just close the window) and reopen.

### Re-test the failing v1.1.2 segments

These are the prompts that failed on v1.1.2; they should all pass now on v1.1.3.

**1. Filters honored** — should return ≤ small number of contacts (not 25 random):
> Search GHL contacts where tag = "qatar" via `mcp__salesmfast-ops__ghl-contacts-reader` with `pageLimit: 50`.

Pass: returns only contacts with the qatar tag, count matches the GHL UI filter.

**2. pageLimit honored** — should return 100, not 25:
> Search GHL contacts via `mcp__salesmfast-ops__ghl-contacts-reader` with `pageLimit: 100`.

Pass: response has `contacts` array of length 100.

**3. Cursor pagination** — should return next page, not the same page:
> Run the search above to get page 1. Then take the last contact's `id` and `dateUpdated`. Convert dateUpdated to millis. Re-run search with `startAfterId: <id>`, `startAfter: <millis>`, `pageLimit: 100`.

Pass: page 2's first contact `id` ≠ page 1's first contact `id`.

**4. Survey submissions** — should return clean envelope, not 404:
> List GHL survey submissions for surveyId `7stMLpd9UTdEviokFW7y` via `mcp__salesmfast-ops__ghl-survey-reader.list-submissions`.

Pass: returns `{ submissions: [...], meta: { total, currentPage, ... } }`. Empty `submissions: []` is also fine if there genuinely are no submissions.

### What's still upstream and not yet fixed

- **GHL forms** are not exposed (no upstream FormsTools class). If you need form submissions specifically, route via the GHL UI export until upstream adds a Forms wrapper.
- **D-01 / D-02** (create-tag / list-tags) work fine on v1.1.3 — your earlier failures were a one-off session-state cascade. If you see them again, fully restart Claude Desktop and try in a fresh chat.

### Verification on Mamoun's machine

✅ Verified end-to-end with `scripts/diag.ts` against the live host. All 6 defect classes pass. Mamoun's test artifacts from the 2026-05-04 session were cleaned up (contact `cV3qN6eskPwfkk1S5byt`, opportunity `JHlOHdqoi19qI3lmKwOI`, diag tag `v113-diag-tmp-tag` all deleted).

Ping me on this ticket once you've re-run the failing segments on v1.1.3. Ruba can resume bundle-by-bundle QA from the start on this version.
